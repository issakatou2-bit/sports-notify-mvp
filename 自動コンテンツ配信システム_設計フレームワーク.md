# 自動コンテンツ配信システム 設計フレームワーク

コレスポ(collespo.com)で構築した仕組みを、他ジャンル・他テーマへ流用するための体系化ドキュメント。

作成: 2026年8月5日

---

## 1. このシステムは何をするものか

一言でいえば、**「公開データを、なぜ今日それが重要かという文脈つきで、毎日自動的に全チャネルへ配信する」**仕組み。

重要なのは、これが「データを一覧表示するだけ」のサービスではないこと。**大量の候補の中から、今日注目すべきものを選び、その理由を説明する**ところに価値がある。人間の編集者がやっていた仕事を、ルールベースのスコアリングとAIの文章化で置き換えている。

### 適用条件

以下の3つが揃えば、ほぼそのまま転用できる。

| 条件 | 理由 |
|---|---|
| **公開APIまたは構造化データが存在する** | 手動入力が必要だと自動化が崩れる |
| **候補が多く、選別に価値がある** | 全部載せれば済むなら、選ぶ仕組みが不要 |
| **「なぜ今日これが重要か」の説明に需要がある** | 単なるデータ配信なら既存サービスに勝てない |

さらに、**大手が手をつけていないニッチ**であることが望ましい。大衆向け領域は、人手とデータを投入した既存サービスが強く、個人開発の入り込む余地が薄い。

---

## 2. アーキテクチャ(6層)

```
┌─────────────────────────────────────────────┐
│ ① 収集層   公開APIから生データを取得              │
├─────────────────────────────────────────────┤
│ ② 評価層   ルールベースでスコアリング(★中核)       │
├─────────────────────────────────────────────┤
│ ③ 検証層   事実確認・矛盾チェック                │
├─────────────────────────────────────────────┤
│ ④ 言語化層 AIが日本語の説明文に変換              │
├─────────────────────────────────────────────┤
│ ⑤ 生成層   サイト/通知/音声/動画/記事へ変換        │
├─────────────────────────────────────────────┤
│ ⑥ 配信層   各チャネルへ自動投稿                 │
└─────────────────────────────────────────────┘
```

### ① 収集層

**原則**: 公式・一次情報のAPIのみを使う。スクレイピングは避ける(規約リスクとレイアウト変更で壊れる)。

**設計上の注意**
- 無料枠のレート制限を把握し、1回の実行での呼び出し回数を数える
- 非公式APIは仕様変更で突然壊れる前提で、防御的に書く
- 取得失敗時に全体を止めない(部分的な欠損を許容する)

### ② 評価層(このシステムの中核)

**「なぜ今日これが重要か」を、複数のルールの合算で数値化する。**

```python
Reason(tag="...", text="表示する理由", weight=2, visible=True)
```

- `weight`: スコアへの寄与
- `visible`: スコアには使うが表示はしない情報を扱える(ブランド力など、
  そのまま見せると薄い情報を、順位付けにだけ使う)

**ルール設計の原則**

1. **1ルール1観点**にする。複合条件を1つのルールに詰め込むと、後で調整できなくなる
2. **閾値を定数として外に出す**。「注目とみなす最低点」は必ず調整対象になる
3. **表示スコアと内部スコアを分ける**。内部スコアは同点時のタイブレークに使う
4. **主観的な重み付けは最小限に**。「この対象は人気だから加点」のような属人的なルールは、
   説明できなくなるうえ、時間が経つと実態とズレる

**実例(コレスポでの失敗と修正)**

| 問題 | 原因 | 修正 |
|---|---|---|
| 注目試合が全体の75%を占めた | 閾値が「0点超え」だった | 閾値を3点に引き上げ、17%に |
| 主力3人在籍と1人在籍が同点 | 人数に関わらず一律+2点 | 人数比例(1人=2, 2人=3, 3人以上=4)へ |
| 別地区なのに「首位攻防戦」 | 同一グループの判定漏れ | 判定条件を追加、別ルールに分離 |

### ③ 検証層

**差分や推論から得た情報は、必ず別ソースで裏を取ってから公開する。**

コレスポでは「所属チームが変わった」という差分を検知した後、

1. 個別APIで現在の所属を問い合わせて一致を確認
2. 「初出場」と書く場合は、試合ログで実際に0件であることを確認
3. どちらか通らなければ**公開しない**

**表現の抑制も検証の一部**。差分から分かるのは「変わった」ことだけで、
その理由(トレード/契約解除/移籍)は分からない。分からないことは書かない。

### ④ 言語化層

**AIに渡すのは「事実」、AIに任せるのは「言い回し」。**

```
【状況】       ← データから機械的に組み立てた事実
【構造的な位置づけ】← 関係性の明示(同一グループか否か等)
【注目理由】    ← ルールベースで抽出した理由
【禁止事項】    ← 過去に起きた誤りを列挙
```

**プロンプトの禁止事項は、実際に起きた誤りを蓄積していく。**
コレスポでは以下が実際に発生し、都度追加した。

- 別グループなのに同一グループのように書く → 所属を明示的に渡す
- 大差で首位なのに「危うい」と煽る → 2位との差を明示的に渡す
- 1試合の敗戦を「1連敗中」と書く → 2以上のときだけ渡す
- 英語名を独自のカタカナに変換する → 「変換禁止、英語のまま」と指示

**教訓**: AIの出力がおかしいとき、原因の多くは**渡しているデータの不足**にある。
プロンプトで叱る前に、判断材料を渡しているか確認する。

### ⑤ 生成層

**1つの素材から、複数の形式を派生させる。**

```
基礎データ(JSON)
  ├→ サイト(HTML + 構造化データ)
  ├→ 日次アーカイブ(日付ごとの独立URL)★SEOの主力
  ├→ 対象別ページ(選手/銘柄/作品など)★検索需要の本命
  ├→ 通知本文(短いフック)
  ├→ SNS投稿(文字数制限あり)
  ├→ ナレーション原稿(話し言葉)
  │    ├→ 音声(TTS)
  │    │    ├→ 動画の音声トラック
  │    │    └→ ポッドキャスト ★追加コストゼロ
  │    └→ 記事下書き
  └→ 動画(縦型ショート / 横型まとめ)
```

**設計上の要点**

- **音声の実測長に合わせて画面の表示時間を決める**。文字数からの推測はズレる
- **フレームはディスクに書かず、ffmpegへ直接パイプする**(生成時間が約25%短縮)
- **画像素材は使わず、プログラムで描画する**。著作権の懸念がなく、品質が安定する
- **8分以上**の動画はミッドロール広告が設置でき、収益性で有利

### ⑥ 配信層

| チャネル | 自動化 | 備考 |
|---|---|---|
| 自サイト | ○ | GitHub Pages |
| プッシュ通知 | ○ | OneSignal(無料枠1万人) |
| Bluesky | ○ | API無料 |
| YouTube | ○ | OAuth必須(初回のみ手作業) |
| ポッドキャスト | ○ | RSS配信、各サービスへの登録は初回のみ |
| X | ✕ | API有料化により手動 |
| note | ✕ | 公式APIなし |
| TikTok/Reels | ✕ | API制限が厳しい |

---

## 3. 運用設計

### 実行基盤

**GitHub Actions**(公開リポジトリなら実行時間無制限=無料)。

**重要な注意点**
- 純正の`schedule`は数時間遅延することがある。時刻の正確性が要るなら
  **外部cron(cron-job.org等)からAPIを叩く**方式にする
- 実行環境には日本語フォントもffmpegも入っていない。明示的にインストールする
- **ジョブ間の依存に注意**。後段のジョブが前段に依存していると、
  末尾の処理(動画など)の失敗で、サイト更新まで巻き添えで止まる
  → 重要度の低い処理は`continue-on-error: true`にする

### ステップ順序の原則

```
1. データ取得
2. 検証
3. 通知・SNS配信      ← ここまでを最短にする
4. サイト構築
5. 音声・動画生成      ← 時間がかかる処理は後ろ
6. 動画投稿
```

**通知は最優先**。サイト構築を待ってから送ると、配信時刻が数分遅れる。
コレスポでは順序見直しで3分20秒→2分25秒に短縮した。

### 期限管理(放置すると必ず止まる)

| 項目 | 頻度 | 切れると |
|---|---|---|
| ドメイン | 年1 | サイトが消える |
| アクセストークン(PAT等) | 年1 | 自動実行が止まる |
| AI APIクレジット | 残高次第 | **静かに**品質が落ちる |
| 静的な人物・銘柄リスト | 随時 | 誤情報が出る |

**最も危険なのはAIクレジット切れ**。エラーにならず、フォールバックで動き続けるため
気づきにくい。自動リロードを少額で有効にしておくのが確実。

---

## 4. 収益化

| 手段 | 特性 |
|---|---|
| **アフィリエイト** | 1件数千円。低流入でも成立しうる。**内容と一致する案件のみ**置く |
| **AdSense** | 1000PVで数百円。流入が育つまで実質ゼロ |
| **動画広告** | 8分以上でミッドロール可 |

**優先順位はアフィリエイト > AdSense**。無関係な広告を貼ると専門性が下がって見え、
かえって逆効果になる。

**法令対応(日本)**
- 広告リンクには**明示的な表記**が必須(ステマ規制、2023年10月〜)
- プライバシーポリシーにアフィリエイト・Cookieの記載
- 運営者情報ページ

---

## 5. 立ち上げの順序(推奨)

```
1. データ取得 + スコアリング     ← ここが価値の源泉。最も時間をかける
2. サイト公開(独自ドメイン)
3. 日付別アーカイブ + sitemap   ← SEOの土台
4. Search Console登録          ← 測定手段。早いほどよい
5. 通知 or SNS配信
6. 対象別ページ                 ← 検索需要の本命
7. 音声・動画                   ← 素材が揃ってから
8. 収益化                       ← 流入が出てから
```

**よくある失敗**: 6〜7を先に作り込んでしまう。
**測定手段(4)を早く入れる**ことが、その後の判断精度を決める。

---

## 6. 設計思想(最重要)

### 検証できることしか書かない

**このシステムの信頼性は、ここに尽きる。**

- 数値は必ずAPIから取得したものを使う。AIに創作させない
- 推論から得た情報は、別ソースで裏を取ってから出す
- 分からないこと(理由・背景)は書かない
- 表記が不確かなもの(人名の読みなど)は、原語のまま出す方がまだ良い

一度でも誤情報を出すと、「自動生成だから信用できない」という評価が定着する。
速度や情報量より、**正確性を優先する**。

### 全て自動、手作業をルーティンにしない

毎日の手作業が1つでもあると、続かない。
初回だけの手作業(OAuth認証など)は許容し、日次の作業はゼロにする。

### 時間を味方にする構造

アーカイブ、対象別ページ、ポッドキャストのエピソードは、
**放っておくだけで毎日厚くなる**。これが効いてくるのは3〜6ヶ月後。

短期の成果が見えなくても、**蓄積が止まらない設計**にしておくことが重要。

---

## 7. 応用候補

| 領域 | データ源 | 「なぜ今日か」の軸 |
|---|---|---|
| 他スポーツ(NBA/NPB/欧州サッカー) | 各リーグAPI | 順位争い、記録接近、注目選手 |
| 株式・決算 | 適時開示、財務API | 決算発表日、業績サプライズ |
| 新刊・新譜 | 出版/音楽DB | 発売日、著者の前作からの期間 |
| ゲームリリース | ストアAPI | 発売日、シリーズ最新作 |
| 学術論文 | arXiv等 | 引用数の急増、著名研究者 |

**共通する問い**: 「候補が大量にある中で、なぜ今日これを見るべきか」

この問いに答えられる領域であれば、本フレームワークがそのまま適用できる。
