# コレスポの共同作業ルール

このフォルダはユーザーがClaudeとCodexで共有している同一の作業コピーです。

1. 作業を始める前に必ず `docs/HANDOFF.md` を読む。必要な範囲で `docs/PLAN.md`、`docs/BACKLOG.md`、`docs/DECISIONS.md` と対象コードを確認する。
2. 編集前に `git status --short` と対象ファイルの差分を確認する。他の担当者の未コミット変更を上書き・取り消ししない。
3. 編集に着手するときはHANDOFFの「作業中」に担当・目的・対象ファイルを記す。他担当と重なる場合は編集を止め、分担を調整する。メモは排他ロックではない。同時編集を避け、書き込み直前に対象を再読する。
4. 作業終了・中断時はHANDOFFを更新する。変更内容、検証したこと、未検証事項、次の一手を簡潔に残し、自分の作業中表示を解除する。
5. コード確認、テスト成功、リモートCI成功、公開結果確認を区別する。過去の担当者の報告を自分の検証結果として扱わない。
6. 決定理由はDECISIONS、未完了の作業一覧はBACKLOG、現在地はHANDOFFへ。全文の会話ログを毎回増やさず、HANDOFFは原則100行以内に保つ。
7. ユーザーは日本語・非技術的な説明を希望。測定値には期間と出典を添え、提案と決定を区別する。APIキーなどの秘密はメモに書かない。
8. 対象はこのプロジェクト。親フォルダや他のプロジェクトを無関係に探索しない。公開や不可逆な変更はユーザーの依頼範囲とDECISIONSの運用ルールに従う。

この規則の追加はユーザーの2026-09-05の依頼によるもの。Claudeとの会話が自動共有されるわけではないため、ファイルに必要な状況を残す。
